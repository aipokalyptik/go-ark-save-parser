// Package arkprofile reads local player profile and tribe save archives.
//
// It opens `.arkprofile` and `.arktribe` files with bounded whole-file reads,
// exposes parsed property containers, and provides typed player, tribe, and
// summary helpers for offline tooling.
package arkprofile
