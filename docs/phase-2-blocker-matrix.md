# Phase 2 Blocker Matrix

Phase 2 is closed for the selected offline, fixture-backed scope. The rows
below are the remaining Phase 2 partial or blocked items from
[`task-inventory.md`](task-inventory.md). They are not requests to expand the
Python oracle suite; they are explicit conditions for future Go-only or
fixture-backed work.

| Row | Current state | Implemented evidence | Reopen only when |
| --- | --- | --- | --- |
| `P2-PROP-003` | Blocked. No chosen offline workflow currently exposes an unported compound payload encoding. | Generic property readers cover declared-size realignment, recoverable overread, aligned malformed compound properties, and map/set/struct body skipping. | A Go parity test, provided save failure, or committed fixture exposes a concrete unsupported compound encoding. |
| `P2-PROP-004` | Partial. Legacy metadata and typed fault handling exist; full legacy object/property parsing is not claimed. | Synthetic legacy archives parse version/object/class metadata into `LegacyArchiveError`; local profile and cluster directory reads preserve valid neighbors and report legacy faults. | A committed fixture or narrowly runnable private case proves exact pre-Unreal-5.5 legacy property/object behavior. |
| `P2-API-005` | Blocked. Modern dino and modern cryopod workflows have typed behavior; legacy/modded cryopod and private pedigree oracle parity are not claimed. | Dino APIs preserve malformed cryopod faults, classify unsupported saddle versions with `ErrUnsupportedCryopodSaddleVersion`, include cryopod item transform fallback, and expose typed pedigree trees. | A concrete supported legacy/modded cryopod fixture appears, or the private pedigree path can produce stable aggregate oracle output without broad Python expansion. |
| `P2-API-006` | Partial. Structure/base local behavior is explicit; upstream `structure_heatmap` numeric parity remains blocked by upstream private-save indexing behavior. | Structure/base export/import round trips, structure health summaries, owner-location skip counters, exact full-parse owner/location API, shared heatmap boundary semantics, and skipped-coordinate heatmap summaries are covered. | A stable upstream/private `structure_heatmap` aggregate can be compared without upstream indexing failure, or a concrete base customization fixture exposes missing local behavior. |
| `P2-API-007` | Blocked. Equipment aggregate, default-stat, and cryopod-saddle fault behavior exist; exact private ranking parity and cosmetics are not claimed. | Ranking candidates, effective internal average stats, inventory-state summaries, quantity/rating averages, curated upstream classification guards, default stat tables, `CustomItemDatas`, unsupported cryopod saddle faults, and an oracle-probe-backed guard for the upstream `equipment_rank` ignore token are covered. | A Go failure or fixture exposes a specific default-table/cosmetic gap, or a narrow focused probe exposes the remaining ranked-row/stat edge without broad Python oracle expansion. |
| `P2-API-008` | Blocked. Local cluster summaries and fault preservation exist; richer fields are fixture-gated. | Uploaded item/dino parse status, identity/stat aggregates, embedded dino level aggregates, item quantity/rating averages, upload-time ranges, JSON/CLI detail output, and directory fault reporting are covered. | New local cluster fixtures expose additional item/dino fields that can be modeled and tested without private data leakage. |
| `P2-API-009` | Blocked. Current player/tribe local and save-contained summaries are typed and fault-preserving; remaining upstream edge cases are fixture-gated. | Player inventory indexing/counting, roster/data summaries, relationship edge counters, local profile death/level/experience helpers, directory maxima/averages, and fault-preserving profile/tribe batch reads are covered. | A chosen offline example, committed fixture, or Go failure exposes a concrete missing player/tribe edge case. |
| `P2-MUT-002` | Blocked by live-server validation scope. Structural mutation-copy behavior exists; live acceptance and semantic authoring are not claimed. | Object removal/upsert, custom-table upsert, generated base/structure/dino/equipment binary export-import round trips, and property-record replacement on copied saves are covered by reopen/reparse tests. | A local-only mutation workflow can be verified structurally, or live-server validation becomes available outside this project scope. |

The accepted-scope evidence audit is
[`acceptance-audit.md`](acceptance-audit.md). The production-readiness review is
[`production-readiness-review.md`](production-readiness-review.md).
